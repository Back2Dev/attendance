import React from 'react'
import { AutoField } from 'uniforms-mui'

// Minimal replacement after removing legacy forms; acts as a pass-through AutoField.
export const CustomAutoField = (props) => <AutoField {...props} />
