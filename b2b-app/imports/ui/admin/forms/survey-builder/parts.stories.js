import React from 'react'
import Parts from './parts'

export default {
  title: 'Survey Builder/Parts',
  component: Parts,
}

const Template = (args) => <Parts {...args} />

export const Default = Template.bind({})
