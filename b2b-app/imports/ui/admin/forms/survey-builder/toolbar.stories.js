import React from 'react'
import Toolbar from './toolbar'

export default {
  title: 'Survey Builder/Builder/Toolbar',
  component: Toolbar,
}

const Template = (args) => <Toolbar {...args} />

export const Default = Template.bind({})
