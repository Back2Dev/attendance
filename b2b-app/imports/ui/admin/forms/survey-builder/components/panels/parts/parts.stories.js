import React from 'react'
import { Parts } from '.'

export default {
  title: 'Survey Builder/Panels/Parts',
  component: Parts,
}

const Template = (args) => <Parts {...args} />

export const Default = Template.bind({})
