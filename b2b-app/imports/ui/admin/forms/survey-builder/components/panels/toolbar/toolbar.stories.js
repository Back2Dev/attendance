import React from 'react'
import { Toolbar } from '.'

export default {
  title: 'Survey Builder/Panels/Toolbar',
  component: Toolbar,
}

const Template = (args) => <Toolbar {...args} />

export const Default = Template.bind({})
