import React, { useState, useContext } from 'react'
import PropTypes from 'prop-types'

const Confirm = props => {
  const { products, loading, productTypes } = props

  return <div>Payment confirmation !!!</div>
}

Confirm.propTypes = {}
export default Confirm
